---
import { Image } from 'astro:assets';
import '../../styles/global.css';

interface Props {
    img?;
    altText?: string;
    imgDescr?: string;
    heading?: string;
    text?: string;
}

const { img, altText = '', imgDescr, heading, text } = Astro.props;
---
<div class="statistics_obj">
    {img && 
        <p class="img_descr">{imgDescr}</p>
        <Image src={img} alt={altText} loading='lazy' class="img"/>
    }
    {!img && 
        <p class="heading">{heading}</p>
        <p class="text">{text}</p>
    }
</div>

<style>
    .statistics_obj {
        display: flex;
        flex-direction: column;
        width: 21.5rem;
        height: 18.125rem;
        font-family: var(--font-inter);
    }

    .img_descr {
        font-size: 1rem;
        color: var(--dark-grey);
        margin-bottom: 0.75rem;
    }

    .heading {
        font-size: 1.5rem;
        margin-bottom: 1.25rem;
    }

    .text {
        display: inline-block;
        font-size: 1rem;
        color: var(--dark-grey);
    }

    .statistics_obj:nth-child(2) {
        width: 16rem;
    }

    .statistics_obj:nth-child(5) {
        width: 16.5rem;
    }
</style>