<section class="eco_hero">
    <div class="letters"></div>
    <div class="subheading">
        <h2>Добро пожаловать</h2>
    </div>
</section>

<style>
    section {
        --hero-background-position: calc((100vw - var(--reduced-width)) / 2) 0;
        --hero-height: 100vh;
        --h2-font-size: 6.8vh;
        --h2-width: 35.5rem;
        --h2-bottom: 0;
        --h2-right: 0%;
        --subheading-top: 24%;
        --subheading-width: var(--reduced-width);
        --subheading-height: 56vh;

    }

    .eco_hero {
        position: relative;
        display: block;
        width: 100vw;
        height: var(--hero-height);
        background: url(../../images/eco/ecoCover.jpg) no-repeat;
        background-size: cover;
        background-position: var(--hero-background-position);
    }

    .letters {
        display: block;
        margin: auto;
        width: var(--reduced-width);
        height: 100%;
        background: url(../../images/eco/eco_letters.png) no-repeat;
        background-size: 100% 100%;
    }

    .subheading {
        display: block;
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        top: var(--subheading-top);
        width: var(--subheading-width);
        height: var(--subheading-height);
    }

    h2 {
        display: inline-block;
        position: absolute;
        width: var(--h2-width);
        font-family: var(--font-lulo-bold);
        font-size: var(--h2-font-size);
        color: white;
        right: var(--h2-right);
        bottom: var(--h2-bottom);
    }

    @media (960px <= width < 1200px) {
        section {
            --hero-height: 33.6875rem;
            --h2-font-size: 2.5rem;
            --h2-width: auto;
            --h2-bottom: 0%;
            --h2-right: 0%;
            --subheading-top: 28%;
            --subheading-width: 56rem;
            --subheading-height: 20rem;
        }

        .letters {
            background: url(../../images/eco/eco_letters_960.svg) no-repeat;
        }
    }

    @media (640px <= width < 960px) {
        section {
            --hero-height: 45rem;
            --h2-font-size: 2.5rem;
            --h2-width: 18.125rem;
            --h2-bottom: 0%;
            --h2-right: 3%;
            --subheading-top: 35%;
            --subheading-width: 40.25rem;
            --subheading-height: 15.375rem;
        }

        .letters {
            background: url(../../images/eco/eco_letters_640.svg) no-repeat;
        }
    }

    @media (480px <= width < 640px) {
        section {
            --hero-height: 16.375rem;
            --h2-font-size: 1.2rem;
            --h2-width: auto;
            --h2-bottom: 2%;
            --h2-right: 3%;
            --subheading-top: 24%;
            --subheading-width: 29rem;
            --subheading-height: 10.25rem;
        }

        .letters {
            background: url(../../images/eco/eco_letters_480.svg) no-repeat;
        }
    }

    @media (320px <= width < 480px) {
        section {
            --hero-height: 30rem;
            --h2-font-size: 1rem;
            --h2-width: 7.25rem;
            --h2-bottom: 0;
            --h2-right: 8%;
            --subheading-top: 36%;
            --subheading-width: 17.5rem;
            --subheading-height: 7.125rem;
        }

        .letters {
            background: url(../../images/eco/eco_letters_320.svg) no-repeat;
            background-position: -20px 0;
        }

        .subheading {
            position: absolute;
            transform: translateX(0);
            left: 6.25%;
        }
    }
</style>